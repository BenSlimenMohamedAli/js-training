// console built in object
console.log("print something in the console");

console.error("print an error in the console");

console.assert(2 == 2, "error"); // it will continue without error
console.assert(2 == 2, "error"); // it will print an error

// Standard Built in Objects
// array => Array;

const array = [1, 3];
array.length;

// String

const s = "fe,d,s,f";

const sa = s.split(",");

console.log(sa);

// Date , RegExp

// Error , SyntaxError, TypeError, RangeError

// Set, Promise, Map, Symbol
